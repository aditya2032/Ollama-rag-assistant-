[project]
name = "ollama-rag-assistant"
version = "1.0.0"
description = "A local, Streamlit-based retrieval-augmented generation assistant powered by Ollama."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama>=0.4.7",
    "streamlit>=1.41.0",
]

[tool.uv]
package = false
